# Day 3 — Multimedia

**Goal / Yool:** Learn to embed video & audio and use responsive media.

**Tasks / Hawsha:**
- Place `sample-video.mp4` and `sample-audio.mp3` in same folder to test.
- Experiment with `playsinline`, `muted`, `loop`, `controls`.
- Try `<picture>` + `<source>` for responsive images.
